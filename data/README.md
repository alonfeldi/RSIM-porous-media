# Local Research Data

Copy large research datasets here when reproducing full experiments locally.

Suggested layout:

```text
data/raw/
data/processed/
```

These folders are ignored by Git. The repository commits only the small example
dataset under `examples/data/`.
