# Legacy Original MATLAB Scripts

This folder contains copies of the original MATLAB scripts from the
author-provided research workspace. They are retained for traceability and are
not the recommended entry points for a fresh reader.

Use the publication-facing scripts in `scripts/` for reproducible runs.

Important safety note: some legacy scripts move, delete, or overwrite data
files. Read them before running them.
